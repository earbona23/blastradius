// A test that exercises 'a' (and thus util, core). Intentionally assertion-free: it is
// a graph fixture, not a real test. 'b' is deliberately left untested.
import { a } from '../src/a.js';
export const _ = a;
